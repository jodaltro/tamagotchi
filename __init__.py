"""Organic Virtual Pet package initialization.

This file makes the `organic_pet_project` directory a Python package so that
its modules can be imported using relative imports (e.g., from
`organic_pet_project.virtual_pet`). It does not need to contain anything
except this docstring.
"""
